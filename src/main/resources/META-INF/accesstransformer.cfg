# The 1.18 entry below made ModelBakery.unbakedCache public so CustomModelLoader could swap in its
# own Map implementation. ForgeModelBakery was deleted in Forge 1.19 and the model pipeline no
# longer hijacks that field, so the AT is no longer needed. The SRG name is 1.18-only and does not
# resolve against 1.20.1 ModelBakery anyway.
# public-f net.minecraft.client.resources.model.ModelBakery f_119212_
